apiVersion: v1
kind: ConfigMap
metadata:
  name: git-context
  annotations:
    config.kubernetes.io/local-config: "true"
    kpt.dev/config-injection: "true"
data:
  provider: example
  org: example
